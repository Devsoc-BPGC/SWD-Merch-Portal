// Grid of merch items, inline editing (name/price/desc fields), view mode with image + badges
// TODO: Port from CSAPortal.jsx
"use client";
export default function MerchItemEditor({ items, isEditing, onChange }) {
  return <div>MerchItemEditor</div>;
}

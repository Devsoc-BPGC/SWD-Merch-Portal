// Single bundle card: title, status, counts, dates, "View Orders" button
// TODO: Port from ClubCoordinatorPortal.jsx
export default function BundleCard({ bundle }) {
  return <div>BundleCard</div>;
}

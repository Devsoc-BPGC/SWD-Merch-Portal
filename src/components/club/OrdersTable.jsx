// Dynamic table: summary row (totals per item), per-order rows with item details
// TODO: Port from ClubCoordinatorPortal.jsx
export default function OrdersTable({ orders, bundle }) {
  return <div>OrdersTable</div>;
}
